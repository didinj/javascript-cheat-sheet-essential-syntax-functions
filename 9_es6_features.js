// ES6+ Features
const [first, second] = [1, 2];
const {name, age} = {name: "Jane", age: 25};

let message = `Hello, ${name}`;
let newArr = [...["apple"], "kiwi"];
let city = person?.address?.city;
let value = input ?? "default";
